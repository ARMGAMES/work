#include "UserDefStructs.h"
#include "Common_atf${ATF_THIN_FILENAME_SUFFIX}.h"
#include "lobby.h"
#include "spinandgo.h"


#define ATF_SAG_MAX_BUYIN_REAL      (5000*100)                 // $5000
#define ATF_SAG_MAX_BUYIN_PM        (PINT32_MAX / 2)
#define ATF_SAG_MAX_BUYIN_FPP        0                         // no FPP for SAG

#define ATF_SAG_MAX_BUYIN_ANY       ATF_SAG_MAX_BUYIN_PM

#define ATF_SAG_MAX_REG_GAMES        10
#define ATF_SAG_MAX_ADMISSIONS       100
#define ATF_SAG_MAX_ADMISSION_PRICE (ATF_SAG_MAX_BUYIN_ANY * ATF_MAX_PM_SCALE_LL)

#define ATF_SAG_MAX_PASSWORD_LEN     0                         // no passwords for SAG




#define TOP_LEVEL_NAMESPACE SagStarter${ATF_THIN_NAMESPACE_SUFFIX}

namespace common
{

	struct RegisterWithScriptId
	{
		PString userId maxlen ATF_USER_NAME_MAX_LENGTH;
		INT32 buyInRM min 0 max ATF_SAG_MAX_BUYIN_REAL;
		INT32 buyInPlay min 0 max ATF_SAG_MAX_BUYIN_PM;
		INT32 buyInFpp min 0 max ATF_SAG_MAX_BUYIN_FPP;
		PString tournPwd maxlen ATF_SAG_MAX_PASSWORD_LEN;
		BYTE useTicket min 0 max 1;
		INT32 buyInT min 0 max ATF_SAG_MAX_BUYIN_REAL;
		INT32 buyInW min 0 max 0; // only 0
		CommMsgBody currencyContext maxsize 10000; // we switched to an empty msgbody because trying to buy into a tournament with a user that does not have auto convert currency set and sending a non-empty currencyContext will return an error
		
		UINT32 sagScriptId;
		
		BYTE numOfGames min 1 max ATF_SAG_MAX_REG_GAMES;
		INT64 admissionPrice min 0 max ATF_SAG_MAX_ADMISSION_PRICE;
		vector<Common${ATF_THIN_NAMESPACE_SUFFIX}::AtfShared::UserTicketDataBase> admissions maxsize ATF_SAG_MAX_ADMISSIONS;
		bool sportsBookVisible parseEndOptB4Me; // PYR-59874
	};
	
};
